package models.enums;

/**
 * Изброим тип {@code ColumnType}, който представя различни видове данни,
 * които могат да се съхраняват в колоните на база от данни.
 */
public enum ColumnType {
    STRING,
    INTEGER,
    DOUBLE,
    FLOAT
}
